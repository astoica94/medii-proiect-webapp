@page
@model WebApplication1.Pages.ITasks.DetailsModel
@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>ITask</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ITask.TaskPriority)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ITask.TaskPriority)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ITask.TaskName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ITask.TaskName)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ITask.CreatedDate)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ITask.CreatedDate)
        </dd>
        @*<dt class="col-sm-2">
            Task items
        </dt>
        <dd class="col-sm-10">
            <table>
                @foreach (var item in Model.TaskItems)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.TaskText)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.TaskDone)
                        </td>
                        <td>
                            <a asp-page="/TaskItems/Edit" asp-route-id="@item.ID">Edit</a> |
                            <a asp-page="/TaskItems/Details" asp-route-id="@item.ID">Details</a> |
                            <a asp-page="/TaskItems/Delete" asp-route-id="@item.ID">Delete</a>
                        </td>
                    </tr>
                }
            </table>
        </dd>*@
    </dl>
</div>
<div>
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.TaskItems[0].TaskText)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.TaskItems[0].TaskDone)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.TaskItems)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.TaskText)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.TaskDone)
                    </td>
                    <td>
                        <a asp-page="/TaskItems/Edit" asp-route-id="@item.ID">Edit</a> |
                        <a asp-page="/TaskItems/Details" asp-route-id="@item.ID">Details</a> |
                        <a asp-page="/TaskItems/Delete" asp-route-id="@item.ID">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
<div>
    <a asp-page="/TaskItems/Create">Add new sub-task</a>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.ITask.ID">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>